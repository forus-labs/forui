/// {@category Widgets}
///
/// A control that allows the user to toggle between checked and unchecked.
library forui.widgets.switch_; // Can't use `forui.widgets.switch` because `switch` is a reserved keyword in dart.

export '../src/widgets/switch.dart';
