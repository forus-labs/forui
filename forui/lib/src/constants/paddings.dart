import 'package:flutter/material.dart';

class FPadding {
  // Small paddings
  static const small = 8.0;
  static const smallVertical = EdgeInsets.symmetric(vertical: small);
  static const smallHorizontal = EdgeInsets.symmetric(horizontal: small);
  static const smallAll = EdgeInsets.all(small);

  // Medium paddings
  static const medium = 16.0;
  static const mediumVertical = EdgeInsets.symmetric(vertical: medium);
  static const mediumHorizontal = EdgeInsets.symmetric(horizontal: medium);
  static const mediumAll = EdgeInsets.all(medium);

  // Large paddings
  static const large = 24.0;
  static const largeVertical = EdgeInsets.symmetric(vertical: large);
  static const largeHorizontal = EdgeInsets.symmetric(horizontal: large);
  static const largeAll = EdgeInsets.all(large);

  // Extra large paddings
  static const extraLarge = 32.0;
  static const extraLargeVertical = EdgeInsets.symmetric(vertical: extraLarge);
  static const extraLargeHorizontal = EdgeInsets.symmetric(horizontal: extraLarge);
  static const extraLargeAll = EdgeInsets.all(extraLarge);

}