/// A class that holds the theme data for the app.
class FThemeData {

  /// Creates a [FThemeData].
  const FThemeData();

  /// Creates a [FThemeData] with light theme.
  const FThemeData.light();

  /// Creates a [FThemeData] with dark theme.
  const FThemeData.dark();
}
