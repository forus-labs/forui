import { Tabs } from 'nextra/components';
import { Widget } from '@/components/demo/widget.tsx';
import LinkBadge from '@/components/ui/link-badge/link-badge.tsx';
import LinkBadgeGroup from '@/components/ui/link-badge/link-badge-group.tsx';

# Badge

A badge draws attention to specific information, such as labels and counts. Use badges to display status, notifications, or small pieces of information that need to stand out.

<LinkBadgeGroup>
    <LinkBadge label="API Reference" href="/api/badge"/>
    <LinkBadge label="Source Code" href="https://github.com/forus-labs/forui/blob/main/lib/src/widgets/badge/badge.dart"/>
</LinkBadgeGroup>

## Overview

<Tabs items={['Preview', 'Code']}>
  <Tabs.Tab>
      <Widget name='badge' query={{}}/>
  </Tabs.Tab>
  <Tabs.Tab>
      ```dart copy
      Row(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [
          FBadge(child: const Text('New')),
          const SizedBox(width: 8),
          FBadge(
            child: const Text('3'),
            style: FBadgeStyle.secondary,
          ),
        ],
      );
      ```
  </Tabs.Tab>
</Tabs>

## Usage

### `FBadge(...)`

```dart copy
FBadge(child: const Text('Badge'));
```

### `FBadge.raw(...)`

```dart copy
FBadge.raw(builder: (context, style) => Text('Badge'));
```

## Examples

### Variants

<Tabs items={['Preview', 'Code']}>
  <Tabs.Tab>
      <Widget name='badge' variant='variants' query={{}}/>
  </Tabs.Tab>
  <Tabs.Tab>
      ```dart copy
      Column(
        children: [
          FBadge(
            child: const Text('Primary'),
            style: FBadgeStyle.primary,
          ),
          const SizedBox(height: 8),
          FBadge(
            child: const Text('Secondary'),
            style: FBadgeStyle.secondary,
          ),
          const SizedBox(height: 8),
          FBadge(
            child: const Text('Outline'),
            style: FBadgeStyle.outline,
          ),
          const SizedBox(height: 8),
          FBadge(
            child: const Text('Destructive'),
            style: FBadgeStyle.destructive,
          ),
        ],
      );
      ```
  </Tabs.Tab>
</Tabs>

### With Counters

<Tabs items={['Preview', 'Code']}>
  <Tabs.Tab>
      <Widget name='badge' variant='counters' query={{}}/>
  </Tabs.Tab>
  <Tabs.Tab>
      ```dart copy
      Row(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [
          FBadge(
            child: const Text('3'),
            style: FBadgeStyle.primary,
          ),
          const SizedBox(width: 8),
          FBadge(
            child: const Text('99+'),
            style: FBadgeStyle.secondary,
          ),
        ],
      );
      ```
  </Tabs.Tab>
</Tabs>

### Custom Styling

<Tabs items={['Preview', 'Code']}>
  <Tabs.Tab>
      <Widget name='badge' variant='custom' query={{}}/>
  </Tabs.Tab>
  <Tabs.Tab>
      ```dart copy
      FBadge.raw(
        builder: (context, style) => Container(
          padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 4),
          decoration: style.decoration,
          child: Text('Custom', style: style.contentStyle.labelTextStyle),
        ),
      );
      ```
  </Tabs.Tab>
</Tabs>
