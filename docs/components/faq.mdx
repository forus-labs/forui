export default function Faq({title, children}) {
    return (
        <details
            open
            className="last-of-type:mb-0 rounded-lg bg-neutral-50 dark:bg-neutral-800 p-2 mt-4"
        >
            <summary>
                <strong className="text-lg">{title}</strong>
            </summary>
            {/* We use children.props.children because of some strange bug that results in the child be nested. */}
            <div className="nx-p-2">{children.props.children}</div>
        </details>
    )
}
