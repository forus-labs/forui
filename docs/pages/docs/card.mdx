import { Tabs } from 'nextra/components';
import { Widget } from "../../components/widget";

# Card
Displays a card typically with a title, subtitle, and child widget.

<Tabs items={['Preview', 'Code']}>
  <Tabs.Tab>
      <Widget query={{}}/>
  </Tabs.Tab>
  <Tabs.Tab>
      ```dart
      FCard(
          title: 'Notification',
          subtitle: 'You have 3 unread messages.',
      );
      ```
  </Tabs.Tab>
</Tabs>

## Usage

### `FCard(...)`
