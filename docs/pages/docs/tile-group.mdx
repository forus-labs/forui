import {Callout, Tabs} from 'nextra/components';
import { Widget } from "../../components/widget";
import LinkBadge from "../../components/link-badge/link-badge";
import LinkBadgeGroup from "../../components/link-badge/link-badge-group";

# Tile Group
A tile group groups multiple [FTile]s.

<LinkBadgeGroup>
    <LinkBadge label="API Reference" href="https://pub.dev/documentation/forui/latest/forui.widgets.tile/forui.widgets.tile-library.html"/>
</LinkBadgeGroup>

<Tabs items={['Preview', 'Code']}>
  <Tabs.Tab>
    <Widget name='tile-group' variant='default' query={{}}/>
  </Tabs.Tab>
  <Tabs.Tab>
    ```dart
    FTileGroup(
      label: const Text('Settings'),
      children: [
        FTile(
          prefixIcon: FIcon(FAssets.icons.user),
          title: const Text('Personalization'),
          suffixIcon: FIcon(FAssets.icons.chevronRight),
          onPress: () {},
        ),
        FTile(
          prefixIcon: FIcon(FAssets.icons.wifi),
          title: const Text('WiFi'),
          details: const Text('Forus Labs (5G)'),
          suffixIcon: FIcon(FAssets.icons.chevronRight),
          onPress: () {},
        ),
      ],
    );
    ```
  </Tabs.Tab>
</Tabs>

## Usage

### `FTile(...)`

```dart
FTileGroup(
  label: const Text('Settings'),
  semanticLabel: 'Settings',
  divider: FTileDivider.partial,
  children: [],
);
```

## Examples

### Full Divider

<Tabs items={['Preview', 'Code']}>
  <Tabs.Tab>
    <Widget name='tile-group' variant='default' query={{divider: 'full'}}/>
  </Tabs.Tab>
  <Tabs.Tab>
    ```dart {3}
    FTileGroup(
      label: const Text('Settings'),
      divider: FTileDivider.full,
      children: [
        FTile(
          prefixIcon: FIcon(FAssets.icons.user),
          title: const Text('Personalization'),
          suffixIcon: FIcon(FAssets.icons.chevronRight),
          onPress: () {},
        ),
        FTile(
          prefixIcon: FIcon(FAssets.icons.wifi),
          title: const Text('WiFi'),
          details: const Text('Forus Labs (5G)'),
          suffixIcon: FIcon(FAssets.icons.chevronRight),
          onPress: () {},
        ),
      ],
    );
    ```
  </Tabs.Tab>
</Tabs>
